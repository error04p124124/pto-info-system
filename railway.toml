[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py demo_data_new && python manage.py collectstatic --noinput && gunicorn pto_info_system.wsgi:application --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
